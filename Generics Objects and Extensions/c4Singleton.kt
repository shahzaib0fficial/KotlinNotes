object studentProgress { // Create object of those predicting only one instance
    var total: Int = 10
    var answered: Int = 3
}

fun main(){
    println("${studentProgress.answered} of ${studentProgress.total} answered.")
}